name: "🎨 Jules // Palette UX Audit"

on:
  schedule:
    - cron: '0 21 * * 0' # Weekly on Sunday at 4:00 PM CDT
  workflow_dispatch:

jobs:
  summon-jules:
    runs-on: ubuntu-latest
    steps:
      - name: "Summoning Palette Agent"
        run: |
          echo "Summoning Jules (Palette) for weekly UX audit..."
          python3 aura/core/jules/palette.py
