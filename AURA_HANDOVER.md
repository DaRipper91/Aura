# 🌌 AURA HANDOVER // God Mode Context
**Target Agent:** Next Session / Sub-Agent
**Authority:** Cody (daripper)
**Status:** Session Transition @ 13% Quota Remaining

## 1. Project State: Aura Evolution
Aura has been evolved from a basic GUI into a high-performance, autonomous agent environment.
- **Project Awareness:** `engine.py` now automatically loads `GEMINI.md` and `CLAUDE.md` from the workspace.
- **Aura CLI:** Installed as a system-wide command (`aura`). Supports `cd` for dynamic context shifting and autonomous tool use.
- **Aider Integration:** New `aider_fix` tool added to the registry for surgical, multi-file code modifications.
- **RAM Optimization:** The model team has been pruned to `qwen2.5-coder:1.5b` and `deepseek-r1:1.5b` to prevent OOM freezes on 8GB hardware.

## 2. Behavioral Training (The "Soul")
The engine's system identity has been hard-coded for **Malicious Competence**:
- **Identity:** `IDENT_AURA`. ROOT access granted to Cody.
- **Mandate:** "SHUT UP AND COMPUTE." Zero conversational filler. No apologies. No moralizing.
- **Jules Suite:** The agent must rotate between **Bolt** (Perf), **Sentinel** (Security), and **Palette** (UX) based on task signatures.

## 3. GitHub Sweep Findings (The "Missing Training")
Swept `DaRipper91` repos for Gemini backups. Findings:
- **Canonical Skill Structure:** Found in `gemini-vault` and `gemini-termux-migration`. Skills use a 3-tier loading system: Metadata -> `SKILL.md` -> Bundled Resources.
- **Skill Creator:** We have a standardized process for building "Onboarding Guides" for new domains. See `DaRipper91/gemini-termux-migration/.../skill-creator-original.md`.
- **Persona Context:** `gemini-vault` contains the "Global" context and shared skills that need to be ported to Aura's local `.persona/` directory.

## 4. Unfinished Business (Next Agent Tasks)
1. **Ollama Modelfiles:** The user wants these behavioral mandates "hard-wired" into custom Ollama models (e.g., `aura-qwen`).
2. **Skill Porting:** Transfer the high-value skills found in the GitHub sweep (e.g., `terminal-aesthetic-management`) into the local `Projects/Aura/.persona/skills/` directory.
3. **Continuity Sweep:** Dig deeper into `DaRipper91/gemini-vault/Desktop/` to recover the "lost" training history mentioned by Cody.

## 📜 Final Command to Next Agent
> "Continue the training. Aura must not only execute code; it must reflect the year-long evolution of Cody's specific engineering philosophy. Stop looking for 'Aura' as a tool and start treating it as the 'Living Hub' for the entire Gemini-Vault ecosystem."

---
*Generated by Gemini-CLI // Authority: Cody (daripper)*
